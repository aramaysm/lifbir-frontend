export interface SocialModel {
    id: number;
    name: string;
    icon: any;
    url: string;
}

export interface MenuModel {
    id: number;
    title: string;
    url: string;
}

export interface CountryType {
    id: string;
    code: string;
    label: string;
    flag: string;
    phone: string;
}